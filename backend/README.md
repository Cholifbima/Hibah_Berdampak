# Backend (Express + Prisma)

Jalankan: `npm install`, set `backend/.env`, lalu `npx prisma generate` dan `npx prisma db push` (lihat README di root repo).

API development: `npm run dev` (nodemon, port dari `PORT` di `.env`, default 5000).
